---
sudo: required
dist: trusty

language: python
python: "2.7"

env:
  - ANSIBLE_VERSION=latest
  - ANSIBLE_VERSION=1.9.0.1
  - ANSIBLE_VERSION=1.8
  - ANSIBLE_VERSION=1.7
  - ANSIBLE_VERSION=1.6

branches:
  only:
    - master

before_install:
  - sudo apt-get update -qq

  # Remove tesseract
  - sudo apt-get remove --purge --yes tesseract-ocr

install:
  # Install Ansible.
  - if [ "$ANSIBLE_VERSION" = "latest" ]; then pip install ansible; else pip install ansible==$ANSIBLE_VERSION; fi

script:
  # Check the role/playbook's syntax.
  - ansible-playbook -i localhost, --connection=local tests/test.yml --syntax-check

  # Run the role/playbook with ansible-playbook.
  - ansible-playbook -i localhost, --connection=local --sudo tests/test.yml -vvvv

  # Run the role/playbook again, checking to make sure it's idempotent.
  - >
    ansible-playbook -i localhost, --connection=local --sudo tests/test.yml 
    | grep -q 'changed=0.*failed=0'
    && (echo 'Idempotence test: pass' && exit 0)
    || (echo 'Idempotence test: fail' && exit 1)
notifications:
  webhooks: https://galaxy.ansible.com/api/v1/notifications/